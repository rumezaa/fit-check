# Python
__pycache__/
*.py[cod]
.venv/

# Node
node_modules/
dist/

# OS / editor
.DS_Store

# Your wardrobe, not the project's. The db and the cutouts live in
# ~/.fit-check/data so every worktree shares one closet — see backend/paths.py
backend/data/
