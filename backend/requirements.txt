# api
fastapi
uvicorn
python-multipart   # required for UploadFile / multipart form parsing
pydantic

# ingestion
rembg[cpu]         # the bare package ships no onnx backend
pillow
pillow-heif        # iphone .heic photos
numpy
scikit-image
scikit-learn

# test only
httpx              # starlette's TestClient imports it
